<h2>You could post picture!</h2>

<h4>name: <%= image_tag(@picture.user.profile_image.icon.url) if @picture.user.profile_image && @picture.user.profile_image.url %><%= @picture.user.name %></h4>

<h4>投稿内容の確認</h4>

<p>image: <%= image_tag(@picture.image.mailer.url) if @picture.image && @picture.image.mailer.url %></p>
<p>content: <%= @picture.content %></p>
