<h1>My favorite posts!!</h1>

<table>
  <tr>
    <th>Poster</th>
    <th>    </th>
    <th>Image</th>
    <th>Content</th>
  </tr>
  <% @favorites.each do |favorite| %>
    <tr>
      <td><%= image_tag(favorite.user.profile_image.icon.url) if favorite.user.profile_image && favorite.user.profile_image.icon.url %></td>
      <td><%= favorite.user.name %></td>
      <td><%= image_tag(favorite.image.thumb.url) if favorite.image && favorite.image.url %></td>
      <td><%= favorite.content %></td>
      <td><%= link_to 'confirm', picture_path(favorite.id) %></td>
    </tr>
  <% end %>
</table>
<br>
<%= link_to "Back to my page", user_path(current_user.id) %>
<br>
<br>
